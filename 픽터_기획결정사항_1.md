# 픽터(Pickter) 서비스 기획 결정사항

> 이 문서는 픽터 서비스의 UX/기능 결정사항을 정리한 문서입니다.
> 데이터 구조 설계 및 구현 시 참고용으로 사용하세요.

---

## 1. 서비스 개요

### 브랜드

**이름**: 픽터 (Pickter)
**의미**: Pick + Filter — "뉴스를 똑똑하게 고르는 사람 / 도구"

**인상**
- 스마트하고 전문적인 이미지
- 개인화와 알고리즘 추천의 느낌 강함
- 브랜드화하기 좋은 중성적인 이름

**타겟 키워드**: 뉴스필터 / AI기반 추천 / 직장인을 위한 큐레이션

### 브랜드 철학

> "우리는 뉴스에 휘둘리기보다, 뉴스를 고를 줄 아는 사람이 되고 싶습니다.
> 픽터는 당신의 기준을 존중하며, 지금 필요한 뉴스만 똑똑하게 선별해 드립니다."

### 온보딩 문구

> **똑똑하게 고르자, 픽터와 함께**
> "정보에 휘둘리지 않고, 정보를 선택하는 사람이 되는 것. 그 시작은 '픽터'입니다."

### 핵심 정체성
- **픽터는 관심뉴스만 추천해주는 뉴스 서비스**
- 사용자의 관심사 기반 큐레이션이 핵심 가치
- 필터버블을 완화하기 위해 "다른 시각" 기능을 통해 동일 이슈의 다양한 관점도 제공

### 차별점
- 단순 뉴스 나열이 아닌 **관심사 기반 추천**
- 동일 이슈에 대한 **다양한 관점(긍정/우려/중립)** 노출
- 능동적 개인화: 사용자가 직접 카테고리 설정·순서 변경 가능

### 철학과 기능의 정합성

개별 기능이 산발적 결정이 아니라 브랜드 철학에서 파생됐음을 보여주는 매핑.

| 철학 요소 | 대응 기능 |
|---|---|
| "고를 줄 아는 사람" | 카테고리 온/오프·순서 설정 (능동적 개인화, 3번) |
| "당신의 기준을 존중" | 명시적 관심사(40%)를 학습 관심사(30%)보다 우선시하는 가중치 설계 (7번) |
| "필요한 뉴스만 선별" | 홈 섹션별 개수 제한(5 / 4 / 3×3 / 3), 정보 밀도 최소화 원칙 (2번) |
| "휘둘리지 않는다" | "다른 시각" 기능 — 필터버블 방지, 한쪽 논조에 매몰되지 않게 (5번) |

"휘둘리지 않는다"는 철학이 "다른 시각" 기능과 가장 직접적으로 맞닿아 있음 — 픽터의 코어 차별점이자 브랜드 철학이 가장 구체적으로 구현된 지점.

### 배경 리서치 데이터

**출처**: 디지털 뉴스 리포트 2025 (로이터저널리즘연구소·한국언론진흥재단), 2021 소셜미디어 이용자 조사 (한국언론진흥재단)

**① 뉴스 회피 — 문제 제기 근거**

| 항목 | 한국 | 48개국 평균 |
|---|---|---|
| 뉴스 회피 경험 | 63% (47개국 중 36위) | — |
| 회피 이유 1위 — 신뢰 불가/편향 | 37% | 29% (+8%p) |
| 회피 이유 2위 — 정치 보도 과다 | 41% | 29% (+12%p, 정치 소재라 슬라이드에서 의도적 제외) |
| 회피 이유 — 많은 양에 피로 | 27% | 31% |
| 회피 이유 — 내 삶과 연관 없어서 | 13% | 18% |

→ **채택**: 신뢰 불가/편향(37%) + 많은 양에 피로(27%) + 내 삶과 연관 없어서(13%). 각각 5번 "다른 시각" 기능 / 2번 정보 밀도 원칙 / 7번 관심사 기반 추천과 1:1 매핑됨. 정치 보도 과다(41%, 격차 최대)는 픽터 기능과 직접 매핑되지 않고 민감 소재라 제외.

**② 뉴스 접근 경로 — 픽터 포지셔닝 근거**

| 항목 | 한국 | 48개국 평균 |
|---|---|---|
| 검색엔진·뉴스수집서비스 의존 | 63% (2위) | 33% |
| 언론사 앱/웹사이트 직접 접근 | 6% (최하위) | 23% |
| 온라인 뉴스 소비 방식 — 읽기 | 55% | 55% |
| 온라인 뉴스 소비 방식 — 보기 | 32% | 31% |
| 온라인 뉴스 소비 방식 — 듣기 | 14% | 15% |

**시계열 추이 (한국, 2019~2025)** — "주된 뉴스 접근 경로"로 응답한 비율

| 경로 | 2019 | 2021 | 2023 | 2024 | 2025 |
|---|---|---|---|---|---|
| 검색엔진·뉴스수집서비스 (포털) | 76% | 72% | 66% | 67% | 63% |
| 소셜미디어 (피드형) | 9% | 12% | 18% | 17% | **19%** |

→ 6년간 포털은 76%→63%로 하락, 소셜미디어(피드형)는 9%→19%로 **2배 이상 증가**. 포털(검색해서 찾는 방식)에서 피드형(알아서 보여주는 방식)으로 무게중심이 이동 중임을 보여주는 핵심 추세.

→ **적용**: "읽기 55%"가 압도적이라는 점이 픽터를 텍스트·카드 중심으로 설계한 근거가 됨(영상·오디오 기능 미포함 결정 뒷받침). 언론사 앱 직접 접근이 최하위(6%)라는 점은 픽터가 "언론사 앱이 아닌 개인화 큐레이터"로 포지셔닝해야 하는 이유로 활용. **포털→피드형 전환 추세는 픽터의 홈 구조(카테고리 카드뉴스 스와이프, 테마별 뉴스)가 "검색이 아니라 피드로 뉴스를 만나는" 최신 습관과 부합함을 보여주는 가장 직접적인 근거.**

**③ 세대별 뉴스 소비 패턴 — 타겟(2030) 포지셔닝 근거**

| 경로 | 20대 | 30대 | 40대 | 50대 | 60대 이상 |
|---|---|---|---|---|---|
| 검색엔진·뉴스수집서비스 | 53% | 64% | 63% | 63% | 68% |
| 소셜미디어 | **29%** | 16% | 19% | 20% | 15% |
| 유튜브 뉴스 이용률 | **44%** | 32% | 48% | 61% | 53% |

→ **핵심 발견**: 20대는 검색 의존도가 전 세대 중 가장 낮고(53%) 소셜미디어(피드형) 의존도는 가장 높음(29%) — "검색해서 찾기"보다 "피드가 보여주는 것"에 익숙한 세대. 픽터의 홈 구조(카드뉴스 스와이프, 테마별 뉴스)가 이 습관과 부합.
→ **반전 포인트**: "2030 = 영상 세대"라는 통념과 달리 유튜브 뉴스 이용률은 20·30대가 오히려 전 세대 중 최저권(44%, 32%)이고 50·60대가 더 높음(61%, 53%). 픽터의 텍스트 중심 설계가 오히려 타겟 세대 습관에 부합한다는 반박 근거로 활용 가능.

---

## 2. 홈화면 구조

> **UI 명칭 변경**: 아래 표의 "섹션" 컬럼은 정책상 내부 명칭이며, 실제 화면 노출명은 "UI 명칭" 컬럼을 따른다. (태그와 동일하게 내부 명칭/UI 라벨을 구분 — 7번 참고)

### 섹션 구성 (고정 순서)

| 순서 | 섹션 (내부 명칭) | UI 명칭 | 기사 개수 | UI 형태 | 비고 |
|---|---|---|---|---|---|
| 1 | 카테고리 카드뉴스 | 카테고리 카드뉴스 | 선택된 탭당 5개 | 가로 스와이프 | 히어로 카드 개념 적용 (7번 참고) |
| 2 | 한눈에 보는 뉴스 | **오늘의 Pick** | **3개** (4개에서 변경) | **세로 블록 쌓임** | 오늘의 주요 뉴스 브리핑 |
| 3 | 테마별 뉴스 | **테마 Pick** | 테마 3개 × 기사 3개 | (형태 미정) | 관심사 키워드 엮은 테마 |
| 4 | 지금 핫한 뉴스 | **지금 인기 핫 Pick** | 3개 | **가로 스와이프, 카드 폭 75~80%** (다음 카드 일부 노출로 스와이프 유도) | 실시간 대중 트렌드 |

### 각 섹션의 역할

**오늘의 Pick (내부 명칭: 한눈에 보는 뉴스)**
- 역할: 오늘의 브리핑
- 기준: 중요도(60%) + 사용자 관심사(40%)
- 갱신 주기: 하루 1~2회
- 개수 변경 이력: 세로 블록 쌓임 UI 특성상 4개는 스크롤 부담이 커서 3개로 축소 — 개수가 적을수록 "엄선된 느낌"이 강조되는 효과도 있음
- 상세 계산 방식은 아래 "한눈에 보는 뉴스 - 중요도 계산" 섹션 참고

**지금 인기 핫 Pick (내부 명칭: 지금 핫한 뉴스)**
- 역할: 실시간 트렌드, 관심사 밖 세상 엿보기 창구
- 기준: 대중 조회수/반응 (개인화 X, 의도적으로 대중성 기반)
- 갱신 주기: 실시간~1시간
- UI 참고: 가로 스와이프인데 카드 개수(3개)가 적으면 화면에 다 걸쳐 보여 스와이프가 무의미해지는 문제가 있었음 → 카드 폭을 75~80%로 키워 다음 카드가 살짝 보이게("peek") 함으로써 개수는 유지하되 스와이프 필요성 확보

**테마 Pick (내부 명칭: 테마별 뉴스)**
- 역할: 관심사 키워드를 엮어 만든 테마 기반 뉴스
- 노출 조건: 사용자 관심사가 3개 이상일 때만 노출 (미만이면 섹션 생략)
- 생성 원칙: **서로 다른 관심 축의 키워드를 교차** (상세는 12번 "테마별 뉴스 생성 정책" 참고)
- 기사 발행 범위: 최근 7일 이내 (UI에 "오늘" 등 시간 표현 미노출)
- ⚠️ **미해결 이슈**: 테마 카드 하나에 제목+부제+키워드 칩 3개+기사 미리보기 3개가 다 들어있어 다른 섹션보다 시각적으로 무거움. UI 형태(가로 스와이프 vs 세로 블록)가 아직 확정되지 않음 — 세로 블록이면 무게 문제가 남고, 가로 스와이프면 완화됨

---

## 3. 카테고리 시스템

### 카테고리 목록 (총 10개)
1. **맞춤** (고정, 끄기/순서 변경 불가)
2. 경제
3. 사회
4. 정치
5. 방송연예
6. 스포츠
7. IT/과학
8. 문화/라이프
9. 세계
10. 오피니언

### 규칙
- **맞춤**: 항상 맨 앞 고정, 비활성화 불가
- **나머지 9개**: 사용자가 개별로 온/오프 가능 + 순서 드래그 변경 가능
- **최소 1개 카테고리는 켜져 있어야 함** (맞춤 외 최소 1개 활성)

### 기본값 (Default)
- **온보딩 경로**: 관심 카테고리 3개 이상 선택받기 → 선택한 것만 활성화된 상태로 시작
- **온보딩 건너뛰기**: 상위 5개 카테고리(경제/사회/정치/방송연예 등) 기본 활성

### 툴팁/안내
- 첫 진입 후 2~3회차에 "카테고리를 맞춤 설정할 수 있어요" 안내 1회 노출
- 온보딩에서 선택한 관심사는 **"맞춤" 추천 알고리즘의 초기 시드**로도 활용

---

## 4. 카드 UI 공통 정책

썸네일 비율, 카드 정보 표시 요소, 시각 표기 방식은 여러 섹션에 걸쳐 적용되는 공통 규칙이므로 한곳에 모아 정의한다.

### 4-1. 썸네일 비율

| 섹션 | 비율 | 이유 |
|---|---|---|
| 카테고리 카드뉴스 | **세로 3:4 직사각형** | 풀스크린 몰입 카드 — 이미지가 주인공 |
| 한눈에 보는 뉴스 | 1:1 정사각형 | 훑는 리스트 — 텍스트·이미지 균형 |
| 테마별 뉴스 | 1:1 정사각형 | 3개 카드 나란히 배치, 가로 스크롤 |
| 지금 핫한 뉴스 | 1:1 정사각형 | 리스트 톤 통일 |
| 다른 시각 (기사상세) | 1:1 정사각형 | 리스트 톤 통일 |
| 연관 이슈 (기사상세) | 1:1 정사각형 | 리스트 톤 통일 |

**원칙**
- 같은 섹션 내 모든 카드의 비율은 반드시 통일 (섞으면 안 됨)
- 섹션 간에는 성격(몰입형 vs 훑기형)에 따라 달라도 됨 — 카테고리 카드뉴스만 예외적으로 세로 직사각형, 나머지는 전부 정사각형으로 통일해 "몰입 카드 vs 훑기 리스트" 위계를 시각적으로 구분

### 4-2. 카드 정보 표시 요소

정보 밀도는 "홈 = 최소화, 상세 = 완결"을 기준으로 삼는다.

| 위치 | 표시 요소 |
|---|---|
| 카테고리 카드뉴스 — 히어로/메인 카드 | 제목 + **추천 태그 2개** + 상대 시간 (카테고리 미노출) |
| 카테고리 카드뉴스 — 일반 카드 | 제목 + 추천 태그 1개 + 상대 시간 (카테고리 미노출) |
| 오늘의 Pick / 지금 인기 핫 Pick / 테마별 뉴스 | 제목 + 추천 태그 1개 + 상대 시간 (카테고리 미노출) |
| "더보기" 전체 리스트 | 제목 + 추천 태그 2개 + 상대 시간 |
| 기사상세 헤드라인 | 제목 + 추천 태그 + 카테고리 + 언론사 + 절대 시간 |

**카테고리를 홈 카드에서 뺀 이유**: 상단 카테고리 탭이 이미 그 역할을 하므로 카드마다 반복 노출하면 중복. 추천 태그가 카테고리보다 정보 가치가 높음(카테고리는 정적 분류라 반복되면 정보량이 0에 가깝지만, 추천 태그는 카드마다 다른 동적 개인화 신호). (단, "맞춤" 탭은 여러 카테고리가 섞여 나오므로 카테고리 노출을 예외적으로 고려 가능)

**히어로/메인 카드가 태그 2개인 이유**: 홈에서 첫인상을 주는 자리라 정보량을 늘려 추천 근거의 신뢰도와 몰입감을 높임. 태그 개수 및 우선순위 선정 로직 상세는 7번 "추천 이유 태그 정책 — 표시 규칙" 참고.

**언론사를 홈 카드에서 뺀 이유**: 훑는 화면에서 인지 부하만 늘림. "어느 언론사인지"는 클릭 이후 궁금해지는 정보라 기사상세에서만 노출.

### 4-3. 시각(時刻) 표기 정책

**홈 — 상대 시간**

| 경과 시간 | 표기 |
|---|---|
| 1분 미만 | 방금 전 |
| 1~59분 | N분 전 |
| 1~23시간 (당일) | N시간 전 |
| 24~47시간 | 어제 |
| 2~6일 | N일 전 |
| 7일 이상 | YYYY.MM.DD |

- 자정을 넘겨도 같은 날 범위(24시간 이내)면 "N시간 전", 날짜가 바뀌면 "어제"로 표기
- 7일 이상은 절대 시간으로 전환 — "8일 전"처럼 역산이 필요한 표현은 오히려 감이 안 와서 담백한 날짜로 대체

**상세 — 절대 시간**

- 포맷: `YYYY.MM.DD 오전/오후 H:MM` (예: `2023.10.11 오전 4:35`)
- 언론사와 함께 표기: `연합뉴스 | 2023.10.11 오전 4:35`

**갱신 방식**
- 상대 시간은 화면 진입 시 1회만 계산 (실시간 갱신 안 함) — 성능 부담 대비 체감 효과가 낮음
- 앱 재진입 시 최신 시각으로 재계산

---

## 5. 기사상세 페이지 구조

### 섹션 구성 (상단 → 하단)
1. **기사 본문** (이 기사)
2. **다른 시각** (같은 이슈, 다른 관점) — 조건부 노출
3. **연관 이슈** (다른 이슈, 내 관심사) — 4개
4. 하단 CTA: "이 카테고리 더 보기" 버튼

### 설계 원칙
- 홈 = 발견, 기사상세 = 깊이
- 깔때기 구조: 이 기사 → 이 이슈 → 내 관심사 확장

---

## 6. "다른 시각" 기능 상세

### 판별 방식
- **실제 구현**: LLM 자동 분류 (기사 본문 → 논조 판별)
- **장기 확장 계획**: 하이브리드 구조 (LLM + 사용자 피드백 + 검증 레이어)

### 분류 기준
각 기사를 다음 3가지 논조로 분류:
- **긍정**: 이슈를 호의적/희망적으로 프레이밍
- **부정(우려)**: 이슈를 비판적/우려 중심으로 프레이밍
- **중립**: 사실 전달 중심, 해석·평가 최소화

### 신뢰도 처리
- LLM confidence가 임계값(예: 80%) 미만이면 **"중립"으로 fallback**
- 애매한 기사는 안전하게 중립 처리해 오분류 리스크 최소화

### UI 표현 (사용자 노출 라벨)
| 내부 분류 | UI 라벨 |
|---|---|
| 긍정 | 긍정적 시각 |
| 부정 | 우려하는 시각 |
| 중립 | 사실 전달 |

- 시각적 구분 보조: 아이콘(🟢🟡⚪) 또는 색상 테두리(초록/주황/회색)

### 노출 조건
- **3가지 시각 모두 확보**: 각 1개씩 총 3개 카드 노출
- **2가지 시각만 확보**: 있는 시각 각 1개씩 총 2개 카드 노출
- **1가지 또는 0개**: 섹션 자체 생략 (바로 "연관 이슈"로 연결)

### 카드 순서 규칙
- 3가지 모두: 긍정 → 중립 → 우려 (고정 순서)
- 2가지 중 정반대 조합(긍정+우려)일 때: **사용자가 읽은 원기사와 반대 시각을 먼저** 배치

---

## 7. 추천 이유 태그 정책 (6개, 확정)

기사마다 **"왜 이게 추천됐는지"**를 알려주는 태그. 홈의 모든 기사 카드(카테고리 카드뉴스·한눈에 보는 뉴스·테마별 뉴스·지금 핫한 뉴스)와 기사상세의 연관 이슈에 공통 적용된다.

**정책상 명칭(내부)과 실제 UI 노출 라벨은 다르다** — 정책 문서·데이터 필드는 아래 "태그" 컬럼 값을 기준으로 하고, 화면에는 "UI 라벨" 컬럼 값을 노출한다.

| 순위 | 태그 (내부 명칭) | UI 라벨 (화면 노출) | 성격 | 노출 방식 | 승격 조건 |
|---|---|---|---|---|---|
| 1 | 연관 | **관련기사** | 세션 기반 | 라벨만 (상세 없음) | — |
| 2 | 관심:[카테고리] | **관심:[키워드]** (예: `관심:AI`) | 명시적 선택 | **키워드 상세 노출** (예: `관심:성수`) | ✅ 키워드 3회 이상 클릭 + 평균 이상 체류시간 → 카테고리 단위에서 키워드 단위로 승격 |
| 3 | 자주 읽는 | 자주 읽는 (동일) | 학습된 행동 | 라벨만 (상세 없음) | ✅ 키워드 5회 이상 → 승격 (단, 라벨엔 상세 미표기, 내부 판단 기준만 승격) |
| 4 | 다른 시각 | 다른 시각 (동일) | 다양성 확보 | 라벨만 | 승격 없음 — 톤 분류 단위 고정 |
| 5 | 발견 | **새로운 발견** | 탐색 유도 | 라벨만 | 승격 없음 |
| 6 | 오늘의 화제 | **오늘의 이슈** | 시의성 | 라벨만 | 승격 없음 |

### 태그별 정의 보충

- **연관 (UI: 관련기사)**: 방금 읽은 기사와 같은 세션 흐름 안에서 자연스럽게 이어지는 이슈
- **관심:[카테고리/키워드] (UI: 관심:[키워드])**: 사용자가 온보딩·설정에서 직접 선택한 명시적 관심사 매칭. 특정 키워드에서 신호가 쌓이면 카테고리 단위(`관심:경제`)에서 키워드 단위(`관심:성수`, `관심:AI` 등)로 상세화됨 — UI에는 항상 승격된 키워드 값이 동적으로 노출됨
- **자주 읽는**: 사용자 행동으로 학습된 관심. 승격되어도 UI에는 구체 키워드를 노출하지 않음 — 사용자가 의식하지 않은 관심까지 드러내면 부담을 줄 수 있어 프라이버시 관점에서 의도적으로 상세 비노출
- **다른 시각**: 원기사와 다른 논조(긍정/우려/사실)의 기사임을 표시. 6번 "다른 시각" 기능의 톤 분류와 동일한 단위 사용, 별도 승격 없음
- **발견 (UI: 새로운 발견)**: 아직 명시/학습 신호가 약하지만 인접 관심사로 확장을 유도하는 태그. 여기서 클릭이 쌓이면 학습 키워드로 축적되어 향후 `자주 읽는` → `관심:[키워드]` 승격 경로의 시작점이 됨
- **오늘의 화제 (UI: 오늘의 이슈)**: 시의성이 높은 이슈. 개인화와 무관하게 노출

### 섹션별 사용 가능 태그

| 섹션 | 사용 가능 태그 |
|---|---|
| 연관 이슈 (기사상세) | 6개 전체 |
| 카테고리 카드뉴스 / 한눈에 보는 뉴스 | 6개 전체 |
| **지금 핫한 뉴스** | **5개만 (`오늘의 화제` 제외)** — 섹션명 자체가 이미 인기·시의성을 설명하므로 카드 태그에서 또 노출하면 동어반복("인기라서 인기"). 아래 "지금 핫한 뉴스 태그 처리" 참고 |
| 테마별 뉴스 | 4개만 (`관심:[키워드]` `자주 읽는` `발견` `다른 시각` — `연관`·`오늘의 화제`는 세션·시의성 기반이라 테마 성격과 안 맞아 제외) |
| 다른 시각 (기사상세) | 태그 미사용 — 긍정적 시각/우려하는 시각/사실 전달 라벨로 대체 |

### 지금 핫한 뉴스 태그 처리

`오늘의 화제`는 우선순위 최하위(6위) — 다른 태그가 하나도 안 맞을 때만 최후 수단으로 노출되는 태그다. 따라서 지금 핫한 뉴스 섹션에서 이 태그만 후보에서 제외하면, 자동으로 다음처럼 동작한다:

- 인기 기사가 사용자의 **명시적 관심사와도 겹치면** → `관심:[키워드]` 노출 (예: "이 인기 기사, 알고 보니 네 관심사이기도 해")
- **학습된 패턴과 겹치면** → `자주 읽는` 노출
- **아무것도 안 겹치면** → 최후 수단으로 `발견` 노출 (승격 조건 없는 범용 태그라 자연스러운 fallback)
- 극단적으로 5개 다 안 맞는 경우만 태그 미노출 (실제로는 `발견`의 범용성 때문에 드묾)

이렇게 하면 "완전히 숨기기"보다 정보 손실이 적다 — 인기 기사가 개인 관심사와 우연히 겹칠 때 그 사실을 알려줄 수 있기 때문. 별도 로직을 새로 만들 필요 없이 기존 우선순위 표에서 후보 하나만 제외하면 되는 구조.

### 표시 규칙

**노출 개수 — 위치별**

| 위치 | 개수 | 설명 |
|---|---|---|
| 히어로/메인 카드 (카테고리 카드뉴스 첫 카드 등) | **2개** | 1순위 + 2순위 태그 동시 노출 |
| 일반 카드 (나머지 모든 홈 카드) | 1개 | 1순위 태그만 노출 |
| **"더보기" 전체 리스트 페이지 (카테고리별 상세)** | **2개** | **카드 크기 상관없이 전체 카드 1순위+2순위 노출** |
| 다른 시각 (기사상세) | 0개 | 태그 미사용, 시각 라벨로 대체 |

**개수별 선정 로직**
- 1개 노출 시: 우선순위(아래 표 "순위" 컬럼) 최상위 태그 1개
- 2개 노출 시: 우선순위 1순위 + 2순위 태그를 나란히 노출. 단, 두 태그가 같은 상위 그룹(예: 1~2위끼리)일 필요는 없음 — 그 기사에 해당하는 태그 중 순위가 높은 두 개를 그대로 사용

**"더보기" 리스트에서 2개를 노출하는 이유**: 히어로 카드는 홈에서 첫인상을 주는 자리라 정보량을 늘려 몰입감을 주는 반면, 일반 카드는 훑는 속도를 위해 1개로 절제한다. 하지만 사용자가 "더보기"를 눌러 전용 리스트로 들어온 시점은 **이미 그 카테고리에 관심을 갖고 더 깊이 탐색하려는 의도**가 확인된 상태이므로, 카드마다 추천 근거를 더 상세히(2개) 보여줘도 정보 과부하가 아니라 오히려 신뢰도를 높이는 효과가 있음.

- **우선순위 기준**: 위 표의 "순위" 컬럼(1~6위)이 곧 단일/복수 태그 선택 우선순위. 해당 기사가 여러 태그 조건에 동시에 해당돼도 순위가 높은 순서대로 필요한 개수만큼만 노출
  - 1~2위(연관·관심): 세션·명시적 신호 — 신뢰도 가장 높음
  - 3~4위(자주 읽는·다른 시각): 학습·다양성 신호 — 중간 신뢰도
  - 5~6위(발견·오늘의 화제): 탐색·시의성 신호 — 명시/학습 신호가 없는 신규 사용자의 fallback (9번 "빈 상태 처리 규칙"과 연결)
- 같은 섹션 내 3개 이상 기사 노출 시, 태그 다양성 확보 원칙 적용 (동일 태그 반복 지양 — 11-6 테마 태그 정책과 동일 원칙)
- 해당 기사에 적용 가능한 태그가 2개 노출 요구 개수보다 적을 경우(예: 1개만 해당) 있는 만큼만 노출

---



## 8. "연관 이슈" 추천 로직

### 노출 개수
- **4개** (모바일 2x2 그리드 또는 가로 스와이프 4장)

### 추천 기준 (가중치)

각 신호의 정의는 아래와 같이 명확히 구분됨:

| 신호 | 가중치 | 정의 |
|---|---|---|
| **사용자 명시 관심사 매칭** | 40% | 사용자가 온보딩/설정에서 **직접 선택**한 카테고리 + 등록한 키워드와의 매칭 |
| **학습된 행동 유사도** | 30% | 사용자가 최근 자주 읽은 기사들의 키워드/엔티티와의 유사도 (열람 이력 기반) |
| **카테고리 정합성** | 20% | 방금 읽은 원기사의 카테고리와 같은 카테고리인가 (문맥 유지용) |
| **최신성** | 10% | 3일~1주일 내 기사인가 |

**중요**: "명시적 관심사"와 "학습된 관심사"는 별개 신호로 분리:
- 명시적: 사용자가 능동적으로 밝힌 관심 → 강한 신호 (40%)
- 학습된: 행동으로 드러난 은근한 관심 → 보조 신호 (30%)
- 이 둘을 분리해야 "내가 관심 있다고 한 적 없는데 왜 자꾸 이 주제가?" 문제 방지

### 필터링 규칙
- **이미 읽은 기사 제외**
- **"다른 시각"과 차별화**: 키워드 100% 겹치는 기사 제외 (연관 이슈는 "같은 관심사 영역의 다른 이슈"여야 함)
- **다양성 규칙**: 같은 기업/인물이 연속으로 2개 이상 나오지 않게

### Fallback 로직 (4개 못 채울 때)
- 1순위: 명시적 관심사 + 학습된 관심사 매칭
- 2순위: 같은 카테고리 내 최신 인기 기사
- 3순위: 전반적 핫 기사

### 추천 이유 태그 (카드에 노출)
7번 "추천 이유 태그 정책"의 6개 태그를 그대로 사용한다. 신호별 매핑은 아래와 같음:

| 매칭 신호 | 태그 |
|---|---|
| 명시적 관심사 매칭 (40%) | `관심:[카테고리/키워드]` |
| 학습된 행동 유사도 (30%) | `자주 읽는` |
| 카테고리 정합성만 매칭 (20%) | `연관` |
| Fallback 2·3순위로 채워진 기사 | `발견` 또는 `오늘의 화제` |

---

## 9. 빈 상태 처리 규칙

### 4가지 원칙
1. 절대 빈 화면을 보여주지 않는다 (Fallback 필수)
2. 빈 상태를 보여야 할 땐 **섹션 자체를 생략**한다
3. 빈 상태가 불가피할 땐 **다음 행동을 제안**한다
4. 사용자에게 **결핍감을 주지 않는다**

### 상황별 처리

| 상황 | 처리 방식 |
|---|---|
| 신규 사용자 (온보딩 X) | 온보딩 유도 배너 + 인기 기사 노출 |
| 초기 사용자 (관심사 적음) | Fallback 로직으로 조용히 채움 |
| 다른 시각 1가지만 | 섹션 생략 |
| 연관 이슈 부족 | Fallback + 추천 이유 태그 차별화 |
| 테마별 뉴스 생성 불가 (관심사 3개 미만) | 섹션 생략 |
| 모든 카테고리 OFF 시도 | 최소 1개 강제 (토스트 알림) |
| 맞춤 다 읽음 | 완료 축하 메시지 + 다음 행동 제안 + 업데이트 시간 안내 |
| 네트워크/서버 오류 | 캐시된 기사 + 재시도 버튼 |

---

## 10. 읽음 처리 & 기사상세 → 홈 복귀

### 읽음 판정 기준
- **기사상세 페이지에서 5초 이상 체류** 또는 **50% 이상 스크롤** 시 "읽음" 처리
- 탭하고 즉시 나간 경우는 "안 읽음"

### 읽은 기사 시각 표시
- **제목 색상 변경**: 검정 → 회색 (주요 방식)
- (선택) 제목 옆 작은 표시 또는 "N시간 전 읽음" 텍스트
- **홈의 모든 섹션에 일관 적용**: 카테고리 카드뉴스, 한눈에 보는 뉴스, 테마별 뉴스, 지금 핫한 뉴스, 연관 이슈

### 스크롤 복원
- 홈 → 기사상세 → 뒤로가기 시 **원래 스크롤 위치 정확히 복원**
- 카테고리 카드뉴스의 경우 **카드 위치(n번째 카드)까지** 복원
- 복귀 시 방금 읽은 카드에 1~2초 하이라이트 애니메이션 (선택)

### 기사상세 최하단 CTA
- **"이 [카테고리명] 더 보기"** 버튼 1개
- 탭하면 해당 카테고리 전체 리스트로 이동

### 뒤로가기
- 좌측 엣지 스와이프 제스처 지원 (iOS 표준)

### 장기 로드맵 (참고)
- "이어서 읽기": 중간까지 읽은 기사에 뱃지 표시
- "오늘/주간 읽은 뉴스 요약" 회고 기능

---

## 11. 한눈에 보는 뉴스 - 중요도 계산

### 중요도(importanceScore)란?
- 단일 지표(조회수 등)가 아닌 **여러 신호를 조합한 계산값**
- 수동 에디터 선정이 아닌 **자동 알고리즘 기반 스코어**
- 기사 단위로 산출되어 `importanceScore` 필드로 저장

### 중요도 구성 신호 (가중치)

| 신호 | 가중치 | 설명 |
|---|---|---|
| 보도량 | 30% | 같은 이슈를 다룬 기사 수 (이슈 클러스터링 기반) |
| 매체 지면 가중치 | 25% | 해당 매체에서 톱기사/1면으로 다뤘는가 |
| 주제 영향력 | 20% | 카테고리별 가중치 (아래 참고) |
| 시의성 | 15% | 오늘/어제 발생한 속보성 여부 |
| 언론사 신뢰도 | 10% | 공신력 있는 매체인가 |

**조회수는 의도적으로 제외**: 대중 조회수 기반은 "지금 핫한 뉴스"에서 다루므로, 역할 분리를 위해 중요도 계산에서 제외함.

### 카테고리별 가중치 (주제 영향력)

연예 기사는 원래 보도량이 매우 많아서, 가중치 조정 없이 두면 브리핑이 연예 뉴스로 도배됨. 이를 방지하기 위한 균형 장치.

**브리핑 가치 2단계 분류 (MVP 단순화 버전):**
- **브리핑 카테고리 (가중치 1.0)**: 정치, 경제, 사회, 세계, IT/과학
- **비브리핑 카테고리 (가중치 0.7)**: 방송연예, 스포츠, 문화/라이프, 오피니언

**세분화 버전 (확장 시):**
| 카테고리 | 가중치 |
|---|---|
| 정치, 경제 | 1.2 |
| 사회, 세계 | 1.1 |
| IT/과학 | 1.0 |
| 문화/라이프 | 0.9 |
| 방송연예, 스포츠 | 0.8 |
| 오피니언 | 0.7 |

### 한눈에 보는 뉴스 최종 공식

```
최종 점수 = 중요도(60%) + 사용자 관심사 매칭(40%)
```

- **중요도 비중이 더 높음**: "꼭 알아야 할 뉴스" 성격이라 중요도 우선
- **관심사 40% 반영**: 관심 없는 뉴스만 나오는 피로감 방지
- **카테고리 카드뉴스와 차별화**:
  - 카테고리 카드뉴스 = 100% 관심사
  - 한눈에 보는 뉴스 = 중요도 60% + 관심사 40%
  - 지금 핫한 뉴스 = 대중성 100%

### Mock 데이터 필드 설계

**`isBriefing: true` 같은 수동 플래그는 사용하지 않음.** 대신 `importanceScore` 필드를 두고 정렬 기반으로 선정.

```typescript
interface Article {
  // 기본 정보
  id: string;
  title: string;
  content: string;
  category: string;
  publishedAt: Date;
  press: string;
  
  // 중요도 계산에 필요한 원천 데이터
  issueId: string;              // 이슈 클러스터 ID (보도량 집계용)
  isFrontPage: boolean;         // 해당 매체에서 톱기사 여부
  pressReliability: number;     // 언론사 신뢰도 점수 (0~1)
  
  // 계산된 최종 점수 (전처리 단계에서 산출)
  importanceScore: number;      // 0~100 정규화된 중요도 점수
}
```

### Mock 데이터 예시

```json
{
  "id": "article_001",
  "title": "정부, 기준금리 0.25%p 인상 발표",
  "category": "경제",
  "issueId": "issue_rate_hike_2025_01",
  "isFrontPage": true,
  "pressReliability": 0.95,
  "importanceScore": 87
}
```

### 구현 전략
- **Mock 단계**: `importanceScore`에 0~100 임의값 할당
- **정렬**: `importanceScore DESC + 관심사 매칭 점수`로 상위 4개 노출
- **실제 계산 로직**: 백엔드 전처리 단계에서 산출 (프론트는 계산된 값을 가정하고 개발)

---

## 12. 테마별 뉴스 생성 정책 (교차 축 원칙)

### 12-1. 핵심 원칙

> **테마는 서로 다른 관심 축의 키워드를 교차시켜 생성한다.**

같은 축 안의 키워드끼리 묶으면(예: 조각투자 + 청약 + 재테크) 결과물이 "경제 뉴스 모음"이 되어 **카테고리 카드뉴스와 기능이 중복**된다. 테마의 존재 이유는 카테고리 브라우징으로는 만날 수 없는 연결을 만드는 것이다.

| 구분 | 조합 예시 | 결과 |
|---|---|---|
| ❌ 동일 축 | 조각투자 + 청약 + 재테크 | 경제 카테고리와 차이 없음 |
| ✅ 교차 축 | 채용시장(IT·과학) + 재테크(경제) | 카테고리로는 못 만나는 연결 |

### 12-2. 생성 규칙

**필수 조건**
- 테마당 **키워드 칩 3개 고정** 노출
  - **교차 축 키워드 2개**: 서로 다른 축에서 선택. 테마 성립의 설계 근거
  - **파생 키워드 1개**: 테마의 구체적 소재를 나타내는 키워드. 사용자가 아직 명시 관심으로 등록하지 않은 인접 키워드를 배치해 **관심사 확장 입구** 역할
- 교차 축 키워드는 사용자의 **명시적 관심 키워드**에서만 조합 (학습 키워드는 기사 선정 단계에서만 활용)
- 파생 키워드는 명시/학습 여부와 무관하게 선택 가능 — 노출·클릭 시 학습 데이터로 축적되어 향후 `관심:[키워드]` 승격 경로가 됨
- 동시 노출되는 3개 테마 간 **키워드 중복 금지**

**축 정의**
- 사용자의 관심 카테고리가 곧 축이 된다 (예: IT·과학 / 문화·라이프 / 경제)

### 12-3. 임팩트 평가 기준

교차한다고 다 좋은 테마는 아니다. 실제 기사가 뒷받침하는 **연결의 종류**로 평가한다.

| 유형 | 정의 | 강도 |
|---|---|---|
| 인과형 | A가 원인, B가 결과로 기사에 명시됨 | ⭐⭐⭐ |
| 체감전환형 | 추상적 이슈가 사용자의 실제 경험 지점으로 구체화됨 | ⭐⭐⭐ |
| 관점반전형 | 같은 대상을 다른 입장에서 보게 됨 | ⭐⭐ |
| 병렬형 | 두 영역이 그냥 같이 언급됨 | ⭐ (탈락) |

병렬형은 "어? 이것도 관련 있네" 수준이라 발견의 가치가 낮다. **인과형·체감전환형 우선.**

### 12-4. 검증 순서 (중요)

> **테마를 먼저 짓고 기사를 찾는 게 아니라, 기사가 실재하는 교차 지점을 먼저 확인한 뒤 테마를 명명한다.**

역순으로 하면 그럴듯한 테마명에 기사를 억지로 끼워 맞추게 되고, 실제 서비스에서 재현 불가능한 설계가 된다.

```
① 교차 후보 조합 도출
② 각 조합에 실제 기사가 존재하는지 검증
③ 기사 3개 이상 확보된 조합만 통과
④ 확보된 기사들의 공통 맥락으로 테마명 명명
⑤ 임팩트 유형 평가 → 병렬형이면 탈락
```

### 12-5. 다양성 규칙

- 동시 노출 3개 테마의 **임팩트 유형이 겹치지 않도록** 배치 (인과 / 체감 / 반전)
- 같은 지역·인물·기업이 여러 테마에 반복 등장하지 않도록 제한
- 특정 축이 3개 테마 모두에 등장하는 것은 허용 (단, 발표·설명 시 의도로 명시)

### 12-6. 태그 정책

테마 섹션은 4개 태그만 사용: `관심:[키워드]` / `자주 읽는` / `발견` / `다른 시각`
(`연관`·`오늘의 화제` 제외 — 세션·시의성 기반이라 테마 성격과 안 맞음)

**태그 배분**: 3개 기사에 서로 다른 태그를 배치. 같은 태그 반복 시 추천 근거가 단조로워 보임.

### 12-7. Fallback

교차 조합으로 테마를 만들 수 없을 때(관심 키워드 3개 미만, 실제 기사 부족 등):
- **테마 섹션 자체를 생략** (9번 빈 상태 정책과 동일)
- 동일 축 조합으로 억지 생성하지 않음 — 카테고리와 중복되는 저품질 테마는 없는 것보다 못함

---

## 13. 테마 확정안 (페르소나: 김도윤)

**페르소나 관심 축**
- IT·과학: AI / 자동화 / 채용시장
- 문화·라이프: 성수 / 팝업스토어 / 전시
- 경제: 조각투자 / 청약 / 재테크

아래 3개 테마는 실제 기사 검증을 거쳐 확정됨. 사용 키워드 6개, 미사용 3개(팝업스토어·조각투자·청약) — 실제 서비스에서도 매일 모든 키워드가 테마로 뜨지 않으므로 자연스러움.

### 테마 1 — 취업이 막막해서, 투자를 시작했다
- **교차**: 채용시장(IT·과학) × 재테크(경제)
- **임팩트 유형**: 인과형
- **부제**: 채용 시장이 왜 얼어붙었고, 2030은 어디로 돈을 옮기고 있는지 알려주는 뉴스
- **키워드 칩**: `채용시장` `재테크` `2030` (앞 2개 = 교차 축, `2030` = 파생)

| 기사 | 태그 |
|---|---|
| "이러다 평생 집 못 사요"…올인한 2030 계좌 봤더니 '대박' | `관심:재테크` |
| 적금 깨는 2030, "투자 아니면 생존이 안 된다" | `발견` |
| 요즘 2030 세대가 돈 공부에 관심 가지는 이유 | `자주 읽는` |

### 테마 2 — 내가 가는 그 매장, 사람이 사라진다
- **교차**: 자동화(IT·과학) × 성수(문화·라이프)
- **임팩트 유형**: 체감전환형
- **부제**: 로봇이 매장을 어떻게 바꾸고, 내 동네 가게는 어떻게 달라지는지 알려주는 뉴스
- **키워드 칩**: `자동화` `성수` `로봇` (앞 2개 = 교차 축, `로봇` = 파생)

| 기사 | 태그 |
|---|---|
| 커피 로봇부터 로봇 빌딩까지, 로봇 전문 기업 엑스와이지 | `관심:성수` |
| 딥핑소스, 피지컬 AI로 리테일 자동화 확대…세븐일레븐 1000개 매장 | `발견` |
| 로봇 자동화, 공장 넘어 매장으로…'리테일 휴머노이드' | `자주 읽는` |

### 테마 3 — 미술관 해설, 이제 AI가 한다
- **교차**: AI(IT·과학) × 전시(문화·라이프)
- **임팩트 유형**: 관점반전형
- **부제**: AI가 전시를 어떻게 바꾸고, 작품 보는 방식은 어떻게 달라지는지 알려주는 뉴스
- **키워드 칩**: `AI` `전시` `도슨트` (앞 2개 = 교차 축, `도슨트` = 파생)

| 기사 | 태그 |
|---|---|
| 경기도미술관, 인공지능 기반 'AI 미술 도슨트' 7월 16일 도입 | `관심:전시` |
| AI 도슨트가 해설, 3D로 작품 감상…'아트테크' 시장 커진다 | `발견` |
| AI와 예술의 공존…달리 미술관의 교육적 아트 경험 | `자주 읽는` |

### 참고: 전체 서사
3개 테마 모두 IT·과학 축 키워드(채용시장/자동화/AI)를 하나씩 포함 → **"기술이 내 관심사 전 영역을 바꾸고 있다"**로 수렴. 의도된 설계이며, 발표 시 이 의도를 명시할 것.

### 13-1. 검증 지표 정의 — "테마 진입 후 연속 탐색"

포트폴리오 검증(Maze UT 등)에서 테마 기능의 효과를 측정할 때 쓰는 지표. **테마 기능 자체의 기여도**를 보기 위한 것이므로, 앱 전체 지표와 분리해서 정의한다.

**정의**: 한 테마 카드 안에서 기사를 **2개 이상 클릭**한 경우

| 항목 | 기준 |
|---|---|
| 테마 진입 | 홈에서 테마 카드에 속한 기사를 최초 클릭한 시점 |
| 연속 탐색 성립 | 같은 테마 카드에 속한 기사를 추가로 1개 이상 더 클릭 (총 2개 이상) |
| 측정 창 | 같은 세션 내 (앱 재실행 전까지) |
| 카운트 제외 | 뒤로가기 없이 같은 기사를 재클릭한 경우는 중복 제외 |

**"세션당 소비 콘텐츠"와의 관계**: 서로 다른 층위의 지표이므로 혼동하지 않는다.

- **세션당 소비 콘텐츠** = 결과 지표. 앱 전체 세션(카테고리 카드뉴스·한눈에 보는 뉴스·연관 이슈 등 모든 경로 포함) 기준으로 "얼마나 많이 읽었나"를 보여줌
- **테마 진입 후 연속 탐색** = 원인 지표. 그 결과 중 **테마 기능이 구체적으로 기여한 부분**만 좁혀서 봄

두 지표를 같은 범위로 잡으면 "테마 덕분인지, 그냥 앱을 오래 써서인지" 구분이 안 되므로, 연속 탐색은 반드시 테마 카드 내부 행동으로 좁혀 측정한다.

```
세션당 소비 콘텐츠 (전체 결과)
    ├─ 테마 진입 후 연속 탐색 → 테마 기능의 기여분
    ├─ 연관 이슈 클릭 → 기사상세 확장의 기여분
    ├─ 다른 시각 클릭 → 균형 잡힌 소비의 기여분
    └─ 카테고리 탭 전환 → 브라우징의 기여분
```

---

## 14. 데이터 구조 설계 시 주요 고려사항

### 핵심 엔티티
아래는 필수로 고려해야 할 엔티티와 주요 속성입니다.

**User (사용자)**
- 관심 카테고리 목록 + 활성화 여부 + 순서
- **명시적 관심 키워드**: 사용자가 직접 등록한 키워드 (온보딩/설정에서 선택)
- **학습된 관심 키워드**: 행동 데이터 기반으로 시스템이 계산한 키워드 (열람 빈도, 최근 상호작용 시점 포함)
- 온보딩 완료 여부
- 툴팁 노출 이력

```typescript
interface User {
  id: string;
  
  // 카테고리 설정 (명시적)
  categorySettings: {
    categoryId: string;
    isActive: boolean;
    order: number;
  }[];
  
  // 명시적 관심사 (사용자가 직접 밝힘)
  explicitKeywords: string[];
  
  // 학습된 관심사 (행동 기반, 시스템이 계산)
  learnedKeywords: {
    keyword: string;
    weight: number;              // 열람 빈도/체류시간 기반
    lastInteracted: Date;
  }[];
  
  // 온보딩 & UI 상태
  onboardingCompleted: boolean;
  tooltipShown: string[];
}
```

**Category (카테고리)**
- 10개 고정 카테고리
- "맞춤"은 특수 카테고리 (고정)

**Article (기사)**
- 기본 정보: 제목, 본문, 카테고리, 발행 시각, 언론사, 이미지 등
- 추출된 엔티티/키워드 (인물, 기업, 지역, 이슈 키워드)
- 논조 분류 값 (긍정/부정/중립) + confidence
- 소속 이슈 ID (같은 이슈로 묶기 위함)

**Issue (이슈)**
- 여러 기사를 하나의 이슈로 묶는 클러스터
- "다른 시각" 기능의 기반

**UserArticleInteraction (사용자-기사 상호작용)**
- 읽음 여부 + 읽은 시각
- 체류 시간, 스크롤 비율
- (선택) 읽다 만 지점 (이어서 읽기 기능 대비)

**Theme (테마)**
- 사용자 관심사 키워드를 엮어 만든 동적 테마
- 테마명, 포함 키워드, 소속 기사 목록

### 추천 시스템이 참조해야 할 데이터
- **명시적 관심사**: 사용자가 켠 카테고리 + 직접 등록한 키워드 (40% 가중치용)
- **학습된 관심사**: 열람 이력 기반 학습 키워드 + 가중치 (30% 가중치용)
- 사용자 읽음 이력 (중복 추천 방지 + 학습 데이터 갱신)
- 기사의 카테고리/키워드/엔티티
- 기사의 최신성 (발행 시각)
- 기사의 importanceScore (한눈에 보는 뉴스용)
- 대중 조회수/반응 데이터 (지금 핫한 뉴스용)

### 논조 분류 파이프라인
- 기사 수집 → LLM 분류 호출 → confidence 체크 → 임계값 미만이면 중립 처리
- 분류 결과는 기사 단위로 저장 + 이슈 단위로도 집계 (같은 이슈 내 분포 파악용)

---

## 15. 포트폴리오 관점 차별화 포인트

데이터 구조/아키텍처를 설계할 때 다음을 강조하면 좋습니다:

- **왜 LLM 분류인가**: 매체 성향 기반은 정치 외 카테고리에 부적합, 10개 카테고리 커버 위해 기사 단위 분류 택함
- **confidence 임계값 처리**: 오분류 리스크 최소화 위해 애매한 건 중립 fallback
- **Fallback 로직**: 빈 상태 방지 + 사용자에게 결핍감 주지 않는 UX 철학
- **확장 가능한 구조**: MVP는 LLM, 장기적으로 하이브리드(사용자 피드백 + 검증 레이어)로 진화
- **테마 교차 축 원칙**: 동일 축 조합은 카테고리 기능과 중복되므로 배제. 테마의 존재 이유를 "카테고리로는 만날 수 없는 연결"로 정의하고, 임팩트 유형(인과/체감전환/관점반전)으로 품질을 평가하는 기준을 세움
- **검증 순서의 역전**: 테마명을 먼저 짓고 기사를 끼워 맞추는 대신, 실제 기사가 존재하는 교차 지점을 먼저 검증한 뒤 명명 → 실서비스 재현 가능성 확보

---

## 참고: 현재 구현 vs 기획 불일치 (개발 전달용)

실제 구현 화면과 기획 대조 중 발견된 항목. 파일(기획)이 아닌 **구현 쪽 수정이 필요한 버그**만 정리.

| # | 항목 | 현재 상태 | 정책 | 우선순위 |
|---|---|---|---|---|
| 1 | `연관`(관련기사) 태그에 키워드 노출 | 일부 카드에 `관련:성수`처럼 키워드가 붙음 (같은 태그의 다른 카드는 정상적으로 `관련기사`만 표시) | 7번 — `연관`은 라벨만, 키워드 상세 노출은 `관심:[키워드]`만 가능 | 중 |
| 2 | 테마 Pick "취업이 막막해서, 투자를 시작했다" 키워드 칩 | `채용시장` `청약` `2030` | 13번 확정안 — `채용시장` `재테크` `2030` (칩과 실제 기사 내용도 청약이 아닌 재테크 관련이라 자체 모순) | 중 |
| 3 | 연관 이슈 섹션 타이틀 | "관심 가질 만한 다른 이슈" | "**당신이** 관심 가질 만한 다른 이슈" | 하 |
| 4 | 지금 인기 핫 Pick 태그가 전부 `오늘의 이슈` | 두 카드 모두 `오늘의 이슈`로만 표시 | 7번 — 이 섹션은 태그 후보에서 `오늘의 화제` 제외, 나머지 5개 중 우선순위 최상위 노출 (개인화 신호 우선 취지가 무력화된 상태) | **상** |

**참고(버그 아님)**: "다른 시각" 3개 기사의 타임스탬프가 전부 동일(`2026.10.11 오전 4:35`) — 정책 위반은 아니나 서로 다른 언론사 기사가 분·초까지 일치하는 건 Mock 데이터 리얼리티 차원에서 부자연스러움.

---

## 참고: 요약 체크리스트

데이터 구조 짤 때 이 목록 빠진 것 없는지 확인하세요:

- [ ] 사용자의 카테고리 설정(활성/순서) 저장
- [ ] 사용자 관심 키워드를 **명시적(explicitKeywords)** vs **학습(learnedKeywords)** 으로 분리 저장
- [ ] 학습 키워드의 weight, lastInteracted 갱신 로직
- [ ] 기사의 논조 분류 값 + confidence 저장
- [ ] 기사의 **importanceScore** (중요도 점수) + 원천 데이터(issueId, isFrontPage, pressReliability) 저장
- [ ] 기사 간 이슈 클러스터링 구조
- [ ] 사용자-기사 읽음 기록 (체류시간/스크롤 포함)
- [ ] 추천 가중치 계산을 위한 인덱싱/조회 전략
- [ ] Fallback 로직이 쿼리 레벨에서 지원되는지
- [ ] 테마 동적 생성 가능 여부
- [ ] 키워드에 **소속 축(카테고리)** 정보가 있어야 교차 조합 판별 가능
- [ ] 테마별 사용 키워드 기록 (동시 노출 테마 간 중복 방지용)
- [ ] 키워드 칩의 **교차 축 / 파생 구분 플래그** (파생 키워드 클릭은 학습 데이터로 축적)
- [ ] 테마 기사 후보 풀을 최근 7일로 제한하는 쿼리 조건
